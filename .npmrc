# This project needs devDependencies (typescript, eslint, vitest, vite) and
# optional dependencies (unrs-resolver native binding for the import resolver).
# `include` takes precedence over any session/user-level NPM_CONFIG_OMIT=dev,
# so `npm install` keeps everything this project needs.
include=prod,dev,optional,peer
